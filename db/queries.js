const pool = require("./pool");

async function addGame(title, release_year, price, genres, platforms){
    // check if game exists
        // if not then add
        // if exists then check if release_
}