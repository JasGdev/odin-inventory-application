const { Router } = require('express');
const gameRouter = Router();
const gameController = require("../controllers/gameController");

// gameRouter.get('/', console.log(1))

module.exports = gameRouter;